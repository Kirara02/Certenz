import 'package:flutter/material.dart';

class NotificationInformationSection extends StatelessWidget {
  const NotificationInformationSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(20),
      child: Text("Inf"),
    );
  }
}
